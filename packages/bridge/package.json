{
  "name": "@xdc-intent/bridge",
  "version": "1.0.0",
  "description": "XDC Intent Framework Bridge Integration — Cross-chain intent fulfillment",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "ethers": "^6.10.0",
    "axios": "^1.6.0",
    "@xdc-intent/types": "workspace:*",
    "@xdc-intent/constants": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
