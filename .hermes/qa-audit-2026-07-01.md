# QA + Release Audit Report

**Date:** 2026-07-01  
**Project:** XDC Intent Framework  
**Commit:** `a528756 feat(deploy): All 5 contracts deployed successfully + frontend addresses updated`  
**Auditor:** Hermes QA + Release Auditor

---

## 0. META

- **Timestamp of this audit:** 2026-07-01 10:14:39 UTC
- **Exact commands used to start the project:**
  - `cd /mnt/c/Users/karan/Desktop/openscans/xdc-intent/packages/frontend && npm run build` (failed)
  - `cd /mnt/c/Users/karan/Desktop/openscans/xdc-intent/packages/contracts && npx hardhat test` (timed out)
  - `cd /mnt/c/Users/karan/Desktop/openscans/xdc-intent/packages/middleware && npm run build` (succeeded)
- **Git branch/commit hash currently checked out:**
  ```
  a528756 feat(deploy): All 5 contracts deployed successfully + frontend addresses updated
  ```
  Working tree has uncommitted changes in:
  - `package-lock.json`
  - `package.json`
  - `packages/frontend/package.json`
  - `packages/frontend/src/hooks/useWallet.ts`

---

## 1. PROJECT SNAPSHOT

**One-line description:**  
A protocol-stack for creating, broadcasting, and settling token-swap "intents" on the XDC Apothem testnet, consisting of Solidity contracts, a TypeScript SDK, an off-chain middleware, and a Next.js frontend.

**Module inventory:**

| Module | Status | Evidence/Notes |
|--------|--------|----------------|
| Solidity contracts (`packages/contracts`) | ❓ NOT VERIFIED | 19 contracts present. Hardhat test suite was started but hung/timed out after 300s before producing output. |
| SDK (`packages/sdk`) | ❓ NOT VERIFIED | Files exist; did not run tests in this session. |
| Middleware (`packages/middleware`) | ✅ VERIFIED WORKING | `npm run build` completed with exit code 0. |
| Frontend (`packages/frontend`) | ❌ BROKEN | `npm run build` fails with `next: not found`; `node_modules/.bin/` is missing and `next` package is marked invalid by npm. |
| DEX contracts (`packages/dex`) | ❓ NOT VERIFIED | Directory exists; not tested this session. |

---

## 2. FUNCTIONAL FLOW TESTING

No core user flow could be executed because the frontend build is broken and the dev server cannot start.

| Flow | Steps executed | Output | Status |
|------|----------------|--------|--------|
| Frontend build / serve | `cd packages/frontend && npm run build` | `sh: 1: next: not found` | ❌ BROKEN |
| Contract test suite | `cd packages/contracts && npx hardhat test` | Process started but produced no output in 300s; killed by timeout | ❌ BROKEN |
| Middleware compile | `cd packages/middleware && npm run build` | `> tsc` — exit code 0 | ✅ VERIFIED WORKING |
| RPC connectivity | `curl -X POST https://apothem.xdcrpc.com -d '{"method":"eth_blockNumber"}'` | `{"result":"0x4fb68e2"}` (block 83,585,250) | ✅ VERIFIED WORKING |

---

## 3. INTEGRATION TESTING

| Integration | Method Used to Test | Status | Evidence |
|-------------|---------------------|--------|----------|
| XDC Apothem RPC (`apothem.xdcrpc.com`) | Direct curl `eth_blockNumber` | ⚠️ PARTIAL | Returns latest block but `eth_getCode` returns `historical state ... is not available`, so the configured contract address cannot be verified on this endpoint |
| XDC Apothem RPC (`earpc.apothem.network`) | Direct curl `eth_blockNumber` + `eth_getCode` | ✅ VERIFIED WORKING | Returns block 83,729,491 and contract code of 15,202 chars for `0x20F97dF1A67D11E4edC863245b34ca9EC35e83Bf` |
| WalletConnect | Code inspection only | ❓ NOT VERIFIED | Project ID is hardcoded as `"YOUR_PROJECT_ID"`; real connection not tested |
| Middleware / API | Did not run server | ❓ NOT VERIFIED | Build passes, but server not started this session |
| Subgraph / indexing | Not present in runnable state | ❓ NOT VERIFIED | Could not locate active subgraph deployment endpoint |

**Hardcoded/mock data flagged:**
- `WALLET_CONNECT_PROJECT_ID = "YOUR_PROJECT_ID"` in `packages/frontend/src/hooks/useWallet.ts` — this is a placeholder, not a real integration.
- `dexPair: "0x69a11B8F6bD5e1b2b2F3E5c4d5e6f7a8b9c0d1e2"` in `packages/frontend/src/lib/contracts.ts` — this is a fake/mock address (repeating hex pattern), not a deployed contract.

---

## 4. ERROR & EDGE CASE HANDLING

| Scenario tested | What happened | Status |
|-----------------|---------------|--------|
| Build with missing `next` binary | `npm run build` fails with `sh: 1: next: not found` | ❌ BROKEN |
| Contract tests on testnet RPC | `npx hardhat test` hangs indefinitely, no output | ❌ BROKEN |
| RPC `eth_getCode` on primary endpoint | Returns `historical state ... is not available` | ❌ BROKEN |
| RPC `eth_getCode` on alternate endpoint | Succeeds, returns real bytecode | ✅ VERIFIED WORKING |

**Unhandled exceptions/crashes found:**
- Frontend cannot be built due to broken npm workspace installation (`next@ invalid`).
- Hardhat test suite does not produce any output in 300s; likely blocked by network RPC timeout or misconfigured hardhat network.

---

## 5. ENVIRONMENT & FREE-TIER CONSTRAINTS

**Required env vars/secrets:**

| Var name | Set? | Placeholder or real? |
|----------|------|----------------------|
| `ETHERSCAN_API_KEY` | ❓ NOT VERIFIED | Cannot read `.env` files; referenced in `packages/contracts/hardhat.config.ts` and `packages/dex/hardhat.config.ts` |
| `CHAIN_ID` | ❓ NOT VERIFIED | Defaults to `51` (Apothem) in middleware and solver |
| `PRIVATE_KEY` / deployer key | ❓ NOT VERIFIED | Referenced in deploy scripts, `.env` is secret-bearing |
| `MIDDLEWARE_SIGNER_KEY` | ❓ NOT VERIFIED | Referenced in middleware |
| `WALLET_CONNECT_PROJECT_ID` | ❌ NOT SET | Hardcoded as `"YOUR_PROJECT_ID"` in frontend |
| `NEXT_PUBLIC_*` | 🚫 NOT IMPLEMENTED | No `NEXT_PUBLIC_` env vars found in frontend code |

**External services:**

| Service | Free-tier? | Limits / notes | Verified? |
|---------|------------|----------------|-----------|
| `apothem.xdcrpc.com` | Yes (public) | Unreliable `eth_getCode` responses; returns "historical state not available" | ⚠️ PARTIAL |
| `earpc.apothem.network` | Yes (public) | Works for `eth_getCode` and block number | ✅ VERIFIED |
| `rpc.apothem.network` | Yes (public) | Times out from this environment | ❌ BROKEN |
| `ws.apothem.network` | Yes (public) | Referenced in frontend for WebSocket; not tested | ❓ NOT VERIFIED |
| Hardhat / Etherscan verify | Requires `ETHERSCAN_API_KEY` | Likely free-tier API key exists | ❓ NOT VERIFIED |

**Paid-tier / credit-card flags:**  
None identified in this session, but `ETHERSCAN_API_KEY` must be valid for contract verification.

---

## 6. GAP LIST FOR "TEST-PRODUCTION READY"

### 🔴 BLOCKER — cannot demo/test live without this
- `packages/frontend` npm workspace is corrupted: `next` package is invalid, `.bin` missing, build fails. Must reinstall dependencies from a clean state.
- `npx hardhat test` hangs with no output. Needs investigation (likely RPC endpoint misconfiguration or network timeout).
- `WALLET_CONNECT_PROJECT_ID` is still `"YOUR_PROJECT_ID"`; WalletConnect will fail.
- `dexPair` address in frontend is a mock address. DEX integration will fail.

### 🟡 IMPORTANT — broken UX or logic bug, not a blocker
- Frontend uses `apothem.xdcrpc.com` which cannot serve `eth_getCode`. Should switch to `earpc.apothem.network` or a reliable endpoint.
- No `NEXT_PUBLIC_` env usage; all contract addresses and RPC URLs are hardcoded in `contracts.ts`.

### 🟢 POLISH — cosmetic/nice-to-have
- Mobile responsiveness not tested.
- No natural-language intent creation (only structured forms).

---

## 7. COMPLETION ESTIMATE

| Module | Status | % Complete | Notes |
|--------|--------|------------|-------|
| Contracts | ❓ NOT VERIFIED | 0% | Tests could not run |
| SDK | ❓ NOT VERIFIED | 0% | Not tested |
| Middleware | ✅ VERIFIED WORKING | 80% | Builds; runtime not tested |
| Frontend | ❌ BROKEN | 0% | Cannot build |

**Overall: ~15% test-production ready**  
Math: 4 modules, 1 builds (80% partial), 1 not tested (0%), 2 broken (0%). Weighted roughly: (0 + 0 + 80 + 0) / 4 = 20%, then reduced because the frontend cannot be served at all = ~15%.

---

## 8. REPRODUCTION STEPS

Run these in your environment to reproduce every check:

```bash
# 1. Meta / git state
cd /mnt/c/Users/karan/Desktop/openscans/xdc-intent
git log -1 --oneline
git status --short

# 2. Frontend build test (expected: FAIL)
cd packages/frontend
npm run build

# 3. Middleware build test (expected: PASS)
cd ../middleware
npm run build

# 4. Contract test test (expected: HANG/FAIL)
cd ../contracts
npx hardhat test

# 5. RPC endpoint tests
curl -X POST https://apothem.xdcrpc.com -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","method":"eth_blockNumber","params":[],"id":1}'
curl -X POST https://apothem.xdcrpc.com -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","method":"eth_getCode","params":["0x20F97dF1A67D11E4edC863245b34ca9EC35e83Bf","latest"],"id":1}'
curl -X POST https://earpc.apothem.network -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","method":"eth_getCode","params":["0x20F97dF1A67D11E4edC863245b34ca9EC35e83Bf","latest"],"id":1}'
```

---

## 9. SUMMARY TABLE

| Module | Status | % Complete | Blockers Count | Last Verified |
|--------|--------|------------|-----------------|----------------|
| contracts | NOT VERIFIED | 0 | 1 | 2026-07-01 |
| sdk | NOT VERIFIED | 0 | 1 | 2026-07-01 |
| middleware | VERIFIED WORKING | 80 | 0 | 2026-07-01 |
| frontend | BROKEN | 0 | 3 | 2026-07-01 |
| dex | NOT VERIFIED | 0 | 1 | 2026-07-01 |

---

**Bottom line:** The frontend is not runnable today due to dependency/workspace corruption. The middleware compiles. The contract test suite hangs. Fix the frontend first.
